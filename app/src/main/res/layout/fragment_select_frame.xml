<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:clipChildren="false"
    android:clipToPadding="false"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="8dp">

    <!-- Área de ajuste: fondo + overlay -->
    <FrameLayout
        android:id="@+id/frameContainer"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipChildren="false"
        android:clipToPadding="false">

        <!-- Imagen de texto, centrada y con wrap_content -->
        <ImageView
            android:id="@+id/imagePreview"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:scaleType="fitCenter"/>

        <!-- Marco móvil, inicialmente invisible -->
        <ImageView
            android:id="@+id/frameOverlay"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:visibility="gone"
            android:scaleType="fitCenter"/>
    </FrameLayout>

    <!-- Botón Confirmar -->
    <Button
        android:id="@+id/btnApplyFrame"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Confirmar viñeta"
        android:layout_marginTop="8dp"/>

    <!-- RecyclerView de marcos -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/frameRecycler"
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:layout_marginTop="8dp"
        android:orientation="horizontal"/>
</LinearLayout>
